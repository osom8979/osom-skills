# 베이스 UI(shadcn/ui 등) 사용

`components/ui/`와 같은 베이스 UI 디렉토리는 **전용 관리자(`shadcn-manager` 스킬)가 소유**합니다. 직접 수정하지 마세요.

필요한 컴포넌트가 아직 없으면 결과 보고에 명시합니다.

```
⚠ 필요한 shadcn/ui 컴포넌트: tooltip, popover
```

이 보고는 `develop` 스킬이 다음 Phase에서 `shadcn-manager`에 위임하는 데 사용됩니다.
