# 패턴 일관성

다음을 점검합니다.

- 기존 코드와 **동일한 패턴**을 사용하는지
- Controlled Component 규칙 (props 기반, 내부 state 금지)을 지키는지
- 페이지 네이밍(`<Name>Page`) 규칙을 지키는지
- Props Optional 규칙을 지키는지

새 패턴을 도입하지 않습니다. 도입이 필요하면 먼저 규칙 문서를 갱신한 후 적용합니다(`osom-rule-manage` 위임).
