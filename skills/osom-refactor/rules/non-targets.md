# 리팩토링 하지 않는 것

다음은 명시적으로 **건드리지 않습니다**.

- 동작하는 코드의 **스타일만** 바꾸는 것 (포매터/린터가 할 일)
- **변경되지 않은 파일**의 "개선" — 변경된 파일만 대상
- **테스트가 없는 코드**의 구조 변경 — 회귀 검증이 불가능
- 단순히 "더 깔끔해 보이는" 변경 — 가치 대비 리스크가 큼

리팩토링은 **확신이 있을 때만** 합니다. 모호한 개선은 리스크만 증가시킵니다.
