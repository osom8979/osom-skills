# 기존 컴포넌트 재사용

이미 존재하는 UI 자산 중 활용 가능한 것을 나열합니다.

- 기존 컴포넌트 디렉토리(`components/`)와 base UI 디렉토리(`components/ui/`)를 모두 훑어 후보를 찾습니다.
- 이름과 경로(예: `@/components/inputs/EmailInput`)를 명시합니다.

신규 컴포넌트 제안은 **재사용 검토 이후에**만 합니다. 같은 기능을 새로 만드는 것은 일관성과 유지보수성을 해칩니다.
