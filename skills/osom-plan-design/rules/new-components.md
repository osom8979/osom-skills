# 필요한 컴포넌트 (신규)

**새로 만들어야 할** UI 요소를 구체적으로 나열합니다.

- 컴포넌트 이름 (제안)
- 카테고리 (예: `inputs`, `cards`, `feedback`, `navigation`)
- 핵심 props / 동작

신규 제안은 [기존 컴포넌트 재사용](component-reuse.md) 검토 **이후**에 합니다 — 재사용 가능한 것을 놓치고 중복 컴포넌트를 만들지 않도록.
