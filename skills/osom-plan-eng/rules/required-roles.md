# 필요한 역할 스킬

`ROLES.md`의 **Enabled roles** 섹션에서 이번 기능에 필요한 역할을 **그 목록 안에서만** 선택합니다.

활성이 아닌 역할(`Enabled roles`에 없는 것)은 **제안하지 마세요** — 분배 단계에서 호출할 수 없습니다. 활성화가 필요하면 별도로 `/osom-init add-role <role>`을 안내합니다.
