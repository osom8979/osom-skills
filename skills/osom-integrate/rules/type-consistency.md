# 타입 정합성

새 타입이 필요하면 다음을 확인합니다.

- 타입 디렉토리(예: `types/`)에 정의되었는지
- 다른 파일에서 올바르게 import하고 있는지
- 빌드 에러가 없는지

```bash
# COMMANDS.md의 Typecheck / Build/Check 명령 사용
<typecheck-command>
```

타입 누락이나 잘못된 import는 직접 수정합니다.
