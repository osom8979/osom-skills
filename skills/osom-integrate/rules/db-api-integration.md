# DB-API 연동

DB 스키마 변경이 있으면 다음을 확인합니다.

- 스키마 변경(컬럼 추가/타입 변경)과 API 변경이 **일치**하는지
- 새 테이블/컬럼에 접근하는 서비스 클라이언트 쿼리가 유효한지
- 마이그레이션이 적용되지 않은 환경에서도 빌드가 깨지지 않는지

DB 함수(RPC) 시그니처 변경은 호출하는 모든 워커/엣지 함수의 인자 타입과 반환 타입이 일치해야 합니다.
