# 성능 고려

다음을 분석합니다.

- 예상 쿼리 패턴 (읽기/쓰기 비율, 조건절 컬럼)
- 필요한 인덱스 (단일/복합, 부분 인덱스, GIN/GIST 등)
- N+1 쿼리 위험
- 대용량 조회 시 페이지네이션·커서 전략

읽기 패턴이 명확하지 않으면 인덱스를 함부로 만들지 않습니다 — 쓰기 비용만 증가합니다. 실제 쿼리 패턴을 먼저 파악한 뒤 인덱스를 제안합니다.
