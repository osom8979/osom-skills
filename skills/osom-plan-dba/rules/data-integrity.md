# 데이터 무결성

다음을 분석합니다.

- 필요한 FK, CHECK, UNIQUE 제약
- 트리거 필요 여부 (예: `updated_at` 자동 갱신, 파생 컬럼 유지)
- enum 도입 시 값 변경 정책 (DROP 어려움)

제약은 **DB 레벨에서** 강제해야 진짜로 무결성이 보장됩니다. 애플리케이션 레벨 검증은 우회 가능하므로 보조 수단으로만 사용합니다.
