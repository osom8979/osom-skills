# 배치 위치

프로젝트의 도메인 구조 중 어디에 속하는지 결정합니다. 번호 밴드/도메인 분류가 있는 프로젝트라면 그 규칙을 따릅니다.

```
postgres/<NN>-<domain>/<sub-domain>/
```

번호는 **실행 순서**를 의미하므로 종속성이 있는 도메인보다 늦은 번호로 배치합니다. 새 도메인이 필요하면 기존 번호 체계와 충돌이 없도록 빈 번호 슬롯을 선택합니다.
