# 보안 샘플링

변경된 파일에서 다음 보안 이슈를 샘플링합니다.

- 하드코딩된 시크릿(API 키, 토큰, 비밀번호)
- XSS 취약점 (`dangerouslySetInnerHTML`, 미검증 사용자 입력 렌더링)
- SQL/NoSQL 인젝션 (raw query에 사용자 입력 직접 삽입)
- 민감한 데이터 로깅

이 항목은 보고만 합니다. 자동 수정하지 않으며, 발견 시 구체적 파일·라인 번호와 함께 명시합니다.
